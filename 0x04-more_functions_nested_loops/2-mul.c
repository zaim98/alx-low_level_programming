#include "main.h"
/**
 * mul - Entry point
 * Description: This program checks if the character is uppercase
 * @a:  the first  argument number of the  function
 * @b: the second argument
 * Return: 1 if uppercase character 0 if it's not
 */
int mul(int a, int b)
{
	return (a * b);
}
