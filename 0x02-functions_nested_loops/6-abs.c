#include "main.h"
/**
 * _abs - Entry point
 * Description: coputes the absulate value of integer
 * Return: absulate valu
 * @n: the number to compute
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
