#include "main.h"
/**
 * _abs - Entry point
 * Description: coputes the absulate value of integer
 * Return: absulate value
 */
int _abs(int n)
{
	if (n <= 0)
	{
		return(n);
	}
	else
	{
		return (n * -1);
	}
}
